# HTTPS Configuration
# For local development (self-signed certificates):
# Leave these commented out - the server will use Backend/certs/ by default

# For production with Let's Encrypt, uncomment and set paths:
# SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem

# Example for Windows with Certbot:
# SSL_CERT_PATH=C:\Certbot\live\yourdomain.com\fullchain.pem
# SSL_KEY_PATH=C:\Certbot\live\yourdomain.com\privkey.pem

# JWT Configuration
JWT_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_Name=webauthn_passkey

# Server Configuration
PORT=5200
NODE_ENV=development

# WebAuthn Configuration
EXPECTED_ORIGIN=https://localhost:5200
EXPECTED_RP_ID=localhost
